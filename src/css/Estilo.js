import { StyleSheet, Dimensions, Platform } from 'react-native'

export default StyleSheet.create({
    Dados:{
        margin: 30,
    },

    DadosTexto:{
        fontSize: 20,
        marginBottom: 5
    }
})